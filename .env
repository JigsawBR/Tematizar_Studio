# Supabase — valores do seu projeto (Project Settings > API Keys)
# Estas VITE_* vão para o NAVEGADOR. Use SOMENTE a chave publishable aqui.
VITE_SUPABASE_URL=https://ltghmkaggaugfydhugjb.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_NeT3ZCxtiuWxpBhm02-Zow_OiTYLfuQ

# A secret key (sb_secret_...) é usada SÓ nas Edge Functions e é injetada
# automaticamente pelo Supabase como SUPABASE_SECRET_KEY. NUNCA a coloque aqui.

# Mercado Pago — PUBLIC KEY (de teste). É pública por design, pode ir ao navegador.
# Obs.: o checkout atual usa Checkout Pro via redirect (init_point), que NÃO
# consome esta chave. Ela fica pronta aqui para quando/ se usar o SDK/Bricks do MP.
VITE_MP_PUBLIC_KEY=APP_USR-7d14e2d8-85c9-4166-9b0a-72c9141c8a33

# Mercado Pago — ACCESS TOKEN é SEGREDO. NUNCA vai aqui/no git. Vai nos secrets do
# Supabase (server-side): npx supabase secrets set MP_ACCESS_TOKEN=...
